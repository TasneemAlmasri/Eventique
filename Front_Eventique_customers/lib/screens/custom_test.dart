import 'package:flutter/material.dart';

class CustomAddTest extends StatelessWidget {
  const CustomAddTest({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Create customized'),
      ),
      body: Center(
        child: Column(
          children: [
            // Text
          ],
        ),
      ),
    );
  }
}